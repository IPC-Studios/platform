# Platform audit — 5 September 2026

Full-stack audit of the IPC Studios platform (Hono API, Postgres/RLS, TanStack
web app). Every row below was confirmed against a file and line before it was
fixed; the "Fix" column names where the fix lives. Gates after the work:
`bun run typecheck` 0 errors, `bun run lint` 0 errors, `bun run test` all
green (pglite migration suite now applies 0001–0035).

## P0 — security and lockout

| # | Finding | Evidence | Fix |
|---|---|---|---|
| P0-1 | Expired-plan lockout: 0033 folded the plan gate into `get_current_company_id()`, so an expired studio could not read its own company row, `create_payment_order()` refused the owner, and `rotate_refresh_token()` revoked the session within 30 min. The renewal page was behind the thing it recovers from. | `0033_auth_hardening.sql:174-186, 278-285`; `0003:167-171`; `0016:453-460` | `0034_plan_gate_and_audit.sql` restores the identity/plan split; pglite tests "plan gate sits on feature access, not identity". |
| P0-2 | Free plan activation: `/subscription/activate` verified the Razorpay signature only when one was sent; the web client never sent one. Real checkout was also impossible (no Razorpay order was ever created). | `subscription/router.ts:42-65`; `routes/subscription.tsx:626-645` | Signature required whenever Razorpay is configured or the environment is not dev-like; `/subscription/order` registers the order with Razorpay; web opens Razorpay Checkout and posts the proof. `lib/razorpay.ts`, `lib/env.ts`, `features/billing/razorpay-checkout.ts`. |
| P0-3 | App hung on "Loading…" forever when `/auth/session` returned anything but 401/403 (a 429 from the shared 10/min `/auth/*` limiter, a 5xx, a network blip). | `AuthProvider.tsx:411-440`; `guards.tsx:550`; `index.ts:61` | Boot uses try/finally and surfaces a retryable error; credential paths and session upkeep have separate limiters. |

## P1 — correctness, hardening, missing features

| # | Finding | Fix |
|---|---|---|
| P1-1 | Rate limiter trusted a client-supplied `CF-Connecting-IP` and its store grew forever. | `lib/client-ip.ts` (configured header, last hop of XFF), bounded `HitStore` with sweep + LRU eviction; tests. |
| P1-2 | 109 swallowed `.catch(() => null)` sites: no log, no correlation id, blanket 400. | `lib/attempt.ts` everywhere (structured log, `X-Correlation-Id`, Sentry-compatible report, pg-code → status). `X-Request-Id` on every request; CORS exposes both ids; the web `ErrorState` shows the reference. Zero swallowed catches remain in the API. |
| P1-3 | CRM mutations had no action gate. | `requireAction('crm', 'create'/'edit'/'delete')` on every CRM write. |
| P1-4 | CRM endpoints returned unparsed rows / `z.any()` / `as any`. | Every CRM endpoint parses request and response against a contract; `any` and the file-level eslint-disables are gone. |
| P1-5 | CSV import split on commas, counted deduped rows as imported, ran per-row transactions. | RFC 4180 parser + header mapping in `@ipc/domain/csv` (tested); `crm_import_leads()` does the whole import in one transaction and reports created / skipped / invalid. |
| P1-6 | Merge forced duplicates to "lost", no undo, UI showed raw UUIDs. | `merged_into` column, `merge_leads` keeps status, `unmerge_leads()`, duplicate groups carry the leads. Undo in the Duplicates tab. |
| P1-7 | Team Dashboard was a copy of Reports. | `crm_team_stats()` + Team tab (per-member open / overdue / due today / uncontacted / hot / new / won / lost / first-response hours). |
| P1-8 | Reports had no date range. | `crm_stats(from, to)` with conversion rate; date range picker with presets. |
| P1-9 | Templates could only be pasted into notes. | `POST /leads/:id/send-template` renders via `@ipc/domain/template`, records the contact, returns a `wa.me` / `mailto:` link; sent from the lead drawer. |
| P1-10 | No automations. | `crm_automation_rules` (trigger + condition + action), evaluated by row trigger and the hourly `run_crm_followup_cron()`; rule editor with a plain-English preview (`describeRule`, tested). |
| P1-11 | Bulk edits had no undo; archived leads could not be seen or restored. | `crm_bulk_patch()` returns the prior snapshot; `crm_restore_leads()`; Undo action on the toast. `is_archived` on the contract, "Show archived" + Restore. |
| P1-12 | `/tasks/my` and `/shoots/my` were ComingSoon placeholders shown to every employee. | Built: `routes/tasks/my.tsx`, `routes/shoots/my.tsx` (+ `GET /shoots/my`). `coming-soon.tsx` deleted. |
| P1-13 | Cloudflare Pages deep links 404'd; no security headers on the SPA. | `apps/web/public/_redirects`, `_headers` (HSTS, CSP, frame denial). |
| P1-14 | Migrations 0032/0033 had no test coverage. | pglite suite applies 0032–0035; 30 new migration tests. |
| P1-15 | Meta handshake ignored `hub.verify_token`; Razorpay webhook parsed JSON unguarded and swallowed ledger failures. | Verify token required (constant-time), JSON guarded, ledger failure → 503 so the provider retries. |
| P1-16 | No audit log, no structured logs, cron history unreachable, health leaked driver errors and the environment name. | `audit_logs` + `audit()` on every mutation; `GET /settings/audit`; `GET /cron/runs`; Settings → System page; `/health` returns `{ok, version, uptime_s, db, db_latency_ms}` with a 2 s probe timeout. |

## P2 — polish, a11y, consistency

| # | Finding | Fix |
|---|---|---|
| P2-1 | Alerts gated on the CRM module. | Nav leaf and page un-gated; `/notifications` removed from the CRM route patterns. |
| P2-2 | Plain-text "Loading…" and inline-styled denial screens. | Shell skeleton on boot; `ModuleRouteGuard` uses `EmptyState`. |
| P2-3 | Projects list and Platform studios tables not sticky. | `table-wrap table-sticky`. |
| P2-4 | CRM tabs not a tablist, tab not in the URL; dialog close had no label. | `role="tablist"` / `tabpanel`, `?tab=` search param; `sr-only` "Close". |
| P2-5 | CRM tabs lacked loading/error states, raw inputs, dead code. | Every tab has skeleton / error / empty states; `follow-ups.tsx` split into `features/crm/tabs/*`. |
| P2-6 | No-op try/finally in `securityHeaders`; duplicated `timingSafeEqual`. | Headers set after `next()` (covers raw Responses; tested); `lib/crypto.ts`. |
| P2-7 | No change-password while signed in. | `POST /auth/change-password` + Settings → Security form. |
| P2-8 | Attendance manual correction "not built yet". | `set_attendance_manual()` + `PUT /hr/attendance/:userId/:date` + Correct dialog; audited. |
| P2-9 | Auth router swallowed register/peek errors; a DB outage read as "invalid invitation". | `attempt()`; outage → 503. |
| P2-10 | No "created" event on a lead's history. | Insert trigger `log_crm_lead_created()`. |
| P2-11 | `/crm/leads` had no cap or archived switch. | `?include_archived`, `?limit` (max 5000, default 2000). |

## Verified and left alone

CORS fails closed in production; the platform-admin console is gated in middleware and again in every RPC; refresh rotation is atomic and single-use; `password_version` revocation works; design tokens (oklch, one radius scale), sidebar collapse persistence, command-palette ranking, dark mode, print styles, mobile card layouts and reduced-motion handling are all in place.

## Known limitations (documented, out of scope)

- ~~The Meta "lead ads" source is a generic JSON webhook plus the verify-token handshake.~~ Closed in the second pass (G5): signed posts, Graph API lead fetch.
- The rate-limit store is per process; run one API replica or move `HitStore` behind a shared store first (RUNBOOK).
- The SPA CSP allows `connect-src https:` because Pages cannot template the API origin; tighten per deployment.


---

# Second pass — 6 September 2026

Re-audited the tree after the first pass. Everything from the first matrix was
re-verified as fixed (no ComingSoon, no `any`, no swallowed catches, gates
green). The gaps below were open and are now closed. Gates after this pass:
typecheck 0, lint 0, 1107 tests (38 files; pglite applies 0001–0036).

| # | Gap | Fix |
|---|---|---|
| G1 | Refresh token readable by any script (localStorage). | Opt-in cookie mode (`AUTH_COOKIE=1`): HttpOnly `ipc_refresh` cookie on `/auth`, body carries an empty token, access token in memory/sessionStorage per tab, CORS credentials on. `lib/session-cookie.ts` + tests; client rotates from either source; sign-in/out felt across tabs. |
| G2 | Saved views browser-only. | `crm_saved_views` (per person) + `GET/POST/DELETE /crm/views`; old local views migrated on first load. |
| G3 | No lead → project conversion. | `convert_lead_to_project()` creates/links the client, creates the project, marks the lead won with `converted_project_id`; `POST /crm/leads/:id/convert`; drawer panel with client match by phone. |
| G4 | No follow-up sequences. | Cadences: `crm_cadences`/`crm_cadence_steps`/`crm_lead_cadences`, `start_lead_cadence`, `stop_lead_cadence`, hourly `crm_advance_cadences` (next follow-up + owner alert + history), auto-stop on won/lost, automation action `start_cadence`, editor on Imports & Automations, panel in the drawer. |
| G5 | Meta lead ads not really wired; WhatsApp link only. | `X-Hub-Signature-256` verified (required for Meta-shaped posts once the secret is set); `leadgen_id` fetched from the Graph API and mapped; WhatsApp Cloud API delivery when configured with `wa.me` fallback; history says "delivered". Tests with mocked fetch. |
| G6 | No SLA. | `crm_settings.sla_hours` (owner-editable on CRM Settings), `crm_team_stats` gains `within_sla`; Team tab shows the share, amber under 80%. |
| G7 | Mutations without audit rows. | tasks status / my status / board order, attendance check-in/out, notification read / read-all, work submit. Public routes (terms ack, webhooks) have their own ledgers. |
| G8 | Partial `aria-invalid`; two spinner pages. | Error text now `role="alert"` with `id`, required inputs carry `aria-invalid` + `aria-describedby` on every form with an error state; accept-invite and terms pages use skeletons; CRM tab bar and toolbars are `no-print`; Reports has a Print button. |
| G9 | `pg_cron` unavailable. | Documented: the `cron` container is the scheduler; cadences ride the same hourly tick. |


---

# Third pass � flagship sprints S1�S4 (6 September 2026)

S1 data model (0038): crm_leads is the deal record � pipeline_id/stage_id with status derived by trigger, contact_id/crm_company_id backfilled one contact per number, close_date/title/currency/score, lost-reason picklist, crm_move_stage() enforcing WIP limits and required fields, phone_norm resync on edit, shared views without the 0036 unique collision, crm_forecast() by stage/owner/month, SLA breach sweep in the hourly tick. Fixes B1�B9.

S2 activities (0039): crm_activities (call/email/meeting/note/task/whatsapp/sms) with actor/contact/task-owner defaults from context, inbound-reply stops cadence, task reminders in the hourly tick; timeline merges events + activities; Twilio click-to-call, .ics meetings, Gmail/O365 sync stubs that fail closed; Integrations card.

S3 workflows (0040): multi-step workflows (action/delay/branch/exit) with field conditions, enrollments, reply auto-exit, outbox drained by the API, lead scoring with hot threshold; legacy single-step rules migrated and retired.

S4 revenue + me (0041): per-studio numbered quotes with GST lines, public accept/decline links, convert carries quote lines into the project; per-person prefs (columns, density, default view); Forecast tab (weighted/by stage/by owner/by month, win rate, cycle); Quotes tab + drawer panel + public accept page; Reports lost analysis. Gates: typecheck 0, lint 0, 1159 tests (43 files; pglite 0001�0041).

