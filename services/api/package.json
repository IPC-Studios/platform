{
  "name": "@ipc/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ipc/contracts": "workspace:*",
    "@ipc/domain": "workspace:*",
    "@ipc/permissions": "workspace:*",
    "hono": "^4.6.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
